services:
  busbot:
    build: .
    container_name: busbot
    restart: unless-stopped
    env_file: .env
    ports:
      - "5000:5000"
    volumes:
      - busbot-data:/app/data

volumes:
  busbot-data:
